@extends('layouts.dashboard')
@section('content')
<div class="flex flex-col gap-6"><div><a href="{{ route('admin.reports.sbfp.attendance') }}" class="inline-flex items-center gap-2 rounded-lg border border-blue-100 bg-blue-50 px-3 py-2 text-xs font-semibold text-blue-700"><i class="fas fa-arrow-left"></i> Back</a><h1 class="mt-3 text-2xl font-bold text-slate-900">{{ date('F', mktime(0,0,0,$reportMonth->month,1)) }} · SY {{ $schoolYear->year }}</h1><p class="mt-1 text-sm text-slate-500">Select a grade level to view sections and attendance summary.</p></div><div class="grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-4">@foreach($grades as $grade)<a href="{{ route('admin.reports.sbfp.attendance.grade', [$reportMonth, $grade]) }}" class="rounded-xl border border-slate-200 bg-white p-5 shadow-sm transition hover:-translate-y-0.5 hover:border-blue-300 hover:shadow-md"><div class="flex items-center justify-between"><span class="flex h-10 w-10 items-center justify-center rounded-lg bg-blue-50 font-bold text-blue-700"><i class="fas fa-users"></i></span><i class="fas fa-arrow-right text-slate-300"></i></div><h2 class="mt-4 font-bold text-slate-900">{{ $grade === 0 ? 'Kinder' : ($grade === 7 ? 'SPED' : 'Grade '.$grade) }}</h2><p class="mt-1 text-xs text-slate-500">Sections and summary</p></a>@endforeach</div></div>
@endsection
