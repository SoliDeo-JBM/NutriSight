@extends('layouts.dashboard')
@section('content')
<div class="flex flex-col gap-6"><div><a href="{{ route('admin.reports.sbfp.attendance.month', $month) }}" class="inline-flex items-center gap-2 rounded-lg border border-blue-100 bg-blue-50 px-3 py-2 text-xs font-semibold text-blue-700"><i class="fas fa-arrow-left"></i> Back</a><h1 class="mt-3 text-2xl font-bold text-slate-900">{{ $grade === 0 ? 'Kinder' : ($grade === 7 ? 'SPED' : 'Grade '.$grade) }} · {{ date('F', mktime(0,0,0,$month->month,1)) }}</h1><p class="mt-1 text-sm text-slate-500">Choose a section or view the grade summary.</p></div><form method="POST" action="{{ route('admin.reports.sbfp.attendance.sections.store', $month) }}" class="flex flex-col gap-3 rounded-xl border border-blue-100 bg-blue-50 p-4 sm:flex-row sm:items-end">@csrf<input type="hidden" name="grade_level" value="{{ $grade }}"><div><label class="mb-1 block text-xs font-semibold text-slate-700">Section</label><input name="section" required placeholder="e.g. A" class="rounded-lg border border-blue-200 px-3 py-2 text-sm"></div><button class="rounded-lg bg-blue-700 px-3 py-2 text-xs font-semibold text-white">+ Add Section</button></form><div class="space-y-2">@forelse($month->sections as $section)<a href="{{ route('admin.reports.sbfp.attendance.section', $section) }}" class="flex items-center justify-between rounded-lg border border-slate-200 bg-white px-4 py-3 text-sm shadow-sm hover:border-blue-300 hover:bg-blue-50"><span class="flex items-center gap-3"><span class="flex h-8 w-8 items-center justify-center rounded-full bg-blue-100 font-bold text-blue-700"><i class="fas fa-layer-group"></i></span><strong>{{ $section->section }}</strong></span><i class="fas fa-arrow-right text-slate-400"></i></a>@empty<p class="rounded-lg border border-dashed border-slate-200 px-5 py-5 text-sm text-slate-500">No sections created for this grade yet.</p>@endforelse<a href="{{ route('admin.reports.sbfp.attendance.grade.summary', [$month, $grade]) }}" class="flex items-center justify-between rounded-lg border border-blue-200 bg-blue-50 px-4 py-3 text-sm font-semibold text-blue-800 hover:bg-blue-100"><span><i class="fas fa-chart-column mr-2"></i>Grade Summary · All Sections</span><i class="fas fa-arrow-right"></i></a></div></div>
@endsection
