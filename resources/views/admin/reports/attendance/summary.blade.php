@extends('layouts.dashboard')
@section('content')
<div class="flex flex-col gap-5"><div><a href="{{ route('admin.reports.sbfp.attendance.grade', [$month, $grade]) }}" class="inline-flex items-center gap-2 rounded-lg border border-blue-100 bg-blue-50 px-3 py-2 text-xs font-semibold text-blue-700"><i class="fas fa-arrow-left"></i> Back</a><h1 class="mt-3 text-2xl font-bold text-slate-900">{{ $grade === 0 ? 'Kinder' : ($grade === 7 ? 'SPED' : 'Grade '.$grade) }} Summary · {{ date('F', mktime(0,0,0,$month->month,1)) }}</h1><p class="mt-1 text-sm text-slate-500">Combined attendance for all sections in this grade.</p></div><div class="overflow-x-auto rounded-xl border border-slate-200 bg-white shadow-sm"><table class="min-w-full border-collapse text-sm"><thead class="bg-slate-800 text-white"><tr><th class="border border-slate-600 px-4 py-3 text-left">Name of Pupil</th>@foreach(range(1,20) as $day)<th class="border border-slate-600 px-2 py-3">{{ $day }}</th>@endforeach</tr></thead><tbody>@forelse($students as $student)<tr class="hover:bg-slate-50"><td class="border border-slate-200 px-4 py-2 font-semibold">{{ $student['name'] }}</td>@foreach($student['days'] as $status)@php($present=in_array(strtolower((string)$status),['present','p','served']))<td class="border border-slate-200 px-2 py-2 text-center {{ $present ? 'font-bold text-emerald-700' : 'text-slate-400' }}">{{ $present ? '✓' : '' }}</td>@endforeach</tr>@empty<tr><td colspan="21" class="px-4 py-10 text-center text-slate-500">No pupils found in this grade.</td></tr>@endforelse</tbody></table></div></div>
@endsection
